import type { LucideIcon } from 'lucide-react';

export interface Feature {
  icon: LucideIcon;
  title: string;
  desc: string;
  iconBg: string;
  iconColor: string;
}

export interface ATApiFeature {
  icon: LucideIcon;
  label: string;
  desc: string;
  iconBg: string;
  iconColor: string;
}
